# ✅ QR-коды готовы к тестированию!

## 🎉 Статус

✅ **Backend запущен** на `http://localhost:8000`
✅ **API работает** и возвращает данные по QR-кодам
✅ **Труба создана** в системе
✅ **QR-коды сгенерированы** в папке `qr_codes/`

## 📱 Как протестировать в мобильном приложении:

### 1. Убедитесь, что backend доступен

Проверьте настройки API URL в мобильном приложении:
- Откройте файл `mobile/.env`
- Убедитесь, что `API_BASE_URL` указывает на правильный адрес:
  - Для физического устройства: `http://192.168.X.X:8000` (IP вашего компьютера)
  - Для эмулятора: `http://10.0.2.2:8000` (Android) или `http://localhost:8000` (iOS)

### 2. Созданные трубы для тестирования:

**Труба 1:**
- QR-код: `PL-COMPANY-38c89ecd-48b1-4595-b9d4-6451a41a4920`
- Файл QR: `qr_codes/qr_COMPANY_38c89ecd.png`
- Статус: ✅ Создана в системе

**Труба 2 (mock данные):**
- QR-код: `PL-COMPANY-e3e96239-aebd-4ce2-a515-7e879a38fdb3`
- Файл QR: `qr_codes/qr_COMPANY_e3e96239.png`
- Статус: ✅ Работает через mock endpoint

### 3. Отсканируйте QR-код

1. Откройте мобильное приложение
2. Перейдите в сканер QR-кодов
3. Отсканируйте QR-код из файла `qr_codes/qr_COMPANY_38c89ecd.png`
4. Должна открыться страница с данными трубы

### 4. Проверка через API

```bash
# Проверка конкретного QR-кода
curl -H "Authorization: Bearer dev-api-key-12345" \
     "http://localhost:8000/api/v1/pipes/qr/PL-COMPANY-38c89ecd-48b1-4595-b9d4-6451a41a4920"
```

## 🔧 Если что-то не работает:

1. **Проверьте логи backend:**
   ```bash
   tail -f /tmp/backend.log
   ```

2. **Проверьте подключение:**
   ```bash
   curl http://localhost:8000/health
   ```

3. **Создайте новую трубу:**
   ```bash
   python3 scripts/create_test_pipe.py --company COMPANY
   ```

4. **Сгенерируйте новый QR-код:**
   ```bash
   python3 scripts/generate_qr.py COMPANY
   ```

## 📂 Файлы QR-кодов:

Все QR-коды находятся в папке: `qr_codes/`

Можно:
- Открыть PNG файл на компьютере
- Отсканировать с экрана
- Или использовать веб-генератор: `http://localhost:3000/qr-generator.html`
