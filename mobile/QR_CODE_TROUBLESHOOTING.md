# Решение проблем с QR-кодами в мобильном приложении

## Проблема: Ошибка подключения к серверу

Если при сканировании QR-кода появляется ошибка подключения, проверьте следующее:

### 1. Проверьте, что backend запущен

```bash
# Проверка через Docker
docker-compose ps

# Или проверка напрямую
curl http://localhost:8000/health
```

Если сервер не запущен:
```bash
# Запустите все сервисы
make up

# Или через docker-compose
docker-compose up -d
```

### 2. Проверьте настройки API URL в мобильном приложении

Откройте файл `.env` в папке `mobile/`:

```bash
cd mobile
cat .env
```

Убедитесь, что `API_BASE_URL` указывает на правильный адрес:

**Для физического устройства:**
```
API_BASE_URL=http://192.168.X.X:8000
```
Где `192.168.X.X` - IP-адрес вашего компьютера в локальной сети.

**Для эмулятора Android:**
```
API_BASE_URL=http://10.0.2.2:8000
```

**Для симулятора iOS:**
```
API_BASE_URL=http://localhost:8000
```

### 3. Убедитесь, что труба существует в базе данных

Создайте тестовую трубу с QR-кодом:

```bash
# Через скрипт
python3 scripts/create_test_pipe.py --company COMPANY

# Или через API напрямую
curl -X POST http://localhost:8000/api/v1/pipes \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer dev-api-key-12345" \
  -d '{
    "company": "COMPANY",
    "manufacturer": "Test",
    "material": "Steel",
    "diameter_mm": 100
  }'
```

### 4. Проверьте QR-код

QR-код должен быть в формате: `PL-COMPANY-UUID`

Примеры валидных QR-кодов:
- `PL-COMPANY-123e4567-e89b-12d3-a456-426614174000`
- `PL-TUTAS-9757a1cd-8292-4535-8e44-979d608a2588`

### 5. Проверьте логи приложения

В консоли Flutter должны появиться логи:
```
🔍 Requesting pipe data for QR code: PL-COMPANY-...
🔍 API Base URL: http://...
✅ Response received: 200
```

Если видите ошибки:
- `Connection refused` - сервер не запущен
- `404 Not Found` - труба с таким QR-кодом не существует
- `401 Unauthorized` - проблема с API ключом

### 6. Быстрое решение

1. Запустите backend:
   ```bash
   make up
   ```

2. Создайте тестовую трубу:
   ```bash
   python3 scripts/create_test_pipe.py
   ```

3. Сгенерируйте QR-код:
   ```bash
   python3 scripts/generate_qr.py COMPANY
   ```

4. Отсканируйте QR-код в приложении

### 7. Проверка через веб-интерфейс

Откройте админ-панель: `http://localhost:3000/admin`

Там можно:
- Создать новую трубу
- Просмотреть QR-код
- Скачать QR-код
